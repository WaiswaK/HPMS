@model IEnumerable<HPMS.Models.Medication>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Medicine)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Medicine)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Medical_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Medical_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Medical_ID })
        </td>
    </tr>
}

</table>
