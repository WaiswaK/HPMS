@model IEnumerable<HPMS.Models.History>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Staff.NIN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Workpace)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Duration)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Staff.NIN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Workpace)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Duration)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HID })
        </td>
    </tr>
}

</table>
