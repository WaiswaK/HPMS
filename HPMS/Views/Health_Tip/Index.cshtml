@model IEnumerable<HPMS.Models.Health_Tip>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tip)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tip)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HT }) |
            @Html.ActionLink("Details", "Details", new { id=item.HT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HT })
        </td>
    </tr>
}

</table>
