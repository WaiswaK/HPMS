@model IEnumerable<HPMS.Models.Reason>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Reason1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Reason1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Reson_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Reson_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Reson_ID })
        </td>
    </tr>
}

</table>
